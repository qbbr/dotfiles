#!/usr/bin/env bash

# grep file path
# {path/to/file}:{line_number}:

REGEX="^(.*):(.*):$"

if [[ "$1" =~ $REGEX ]]; then
    phpstorm --line echo "${BASH_REMATCH[2]}" "${BASH_REMATCH[1]}" &
else
    phpstorm $1 &
fi
