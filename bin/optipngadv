#!/usr/bin/env bash

# ./optipngadv
#
# using:
#   ./script file.png
#   for i in *; do ./script $i; done
#
# depends: optipng, advancecomp
# author: @qbbr

optipng -o7 "$1" && advpng -z4 "$1" && advdef -z4 "$1"
